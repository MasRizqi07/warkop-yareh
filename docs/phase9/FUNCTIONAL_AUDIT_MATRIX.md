# Phase 9 Functional Audit Matrix

Baseline audited: `a12d52283a087e4f2fad56748b4452b8d83d10a5` (`main`)

Audit date: 2026-09-15; implementation evidence refreshed 2026-09-16
(Asia/Jakarta)

Status: **verified-with-gaps** — source, isolated persistence, local browser E2E,
and route/UI audit are verified; exact-commit CI, staging walkthroughs, and owner
Phase 5 sign-off remain separate gates.

The prompt expected 28 web routes. The baseline actually contains **35** files matching `apps/web/src/app/**/page.tsx` and **21** matching `apps/admin/src/app/**/page.tsx`. All 56 are enumerated below.

## Reading the matrix

- `Y — editorial/config only` means static explanatory copy, route configuration, fallback imagery, or UI taxonomy is present but no operational record is fabricated.
- `Y — GAP` means static or disconnected behavior affects a product claim and needs a scope decision or remediation.
- A handler reference means the client call was traced to the matching NestJS controller method; it is source evidence, not proof that the deployed service or database currently works.
- The last column preserves the Phase 0 manual/staging status. Automated local
  route coverage and mutation proof are indexed separately below; an automated
  visit is not relabeled as a manual staging walkthrough.
- Phase 1-4 runtime evidence is in
  [PHASE1_4_EVIDENCE.md](./PHASE1_4_EVIDENCE.md).

## Web routes (35)

| Route | Primary user action(s) on page | Data source (API endpoint(s) hit, verified by grep + reading the handler) | Static/mock content present? (Y/N + line ref) | Buttons/forms with no-op or unwired handlers (Y/N + line ref) | Manually exercised end-to-end in dev environment? (Y/N) |
|---|---|---|---|---|---|
| `/` | Select branch; browse featured menu; open booking/loyalty; read reviews | `GET /branches`, `GET /catalog?branchId`, `GET /content/reviews` via `catalog.api.ts:13-22`, `public.api.ts:119-120`; handlers `branch.controller.ts:31-35`, `catalog.controller.ts:42-54`, `content.controller.ts:27-31` | Y — editorial hero/telemetry labels only (`(marketing)/page.tsx:59,96-102`); operational cards are API-backed | N — branch selector and retry/link actions are wired (`page.tsx:154-157,191-195`) | N |
| `/about` | Read brand story and values | No operational API; intentionally editorial | Y — editorial content (`about/page.tsx:10-152`), not mock operational data | N — no form or operational mutation | N |
| `/blog` | Search/filter published articles; open article | `GET /content/blog`; client `public.api.ts:108-112`; handler `content.controller.ts:14-18` | Y — fallback image map only (`blog/page.tsx:13-17`); article records are API-backed | N — search/category/link interactions wired (`blog/page.tsx:22-32`) | N |
| `/blog/[slug]` | Read a published article; return to list | `GET /content/blog/:slug`; client `public.api.ts:115-116`; handler `content.controller.ts:21-24` | Y — fallback hero image (`blog/[slug]/page.tsx:18`); article data is API-backed | N — navigation is wired (`blog/[slug]/page.tsx:18`) | N |
| `/booking` | Select branch/package/date/table/add-ons; quote; reserve/pay; cancel; export calendar | `GET /branches`; `GET /bookings/catalog`; `POST /bookings/quote`; `GET /bookings/availability`; `POST /bookings`; `GET /reservations`; `PATCH /reservations/:id/status`; `POST /payments/midtrans/snap` (`booking-page.tsx:46-79`); handlers `booking.controller.ts:25-53`, `reservations.controller.ts:68-108`, `payment.controller.ts:41-56` | Y — 14-day ribbon and default package key are UI configuration (`booking-page.tsx:35-57`); prices/tables/history are API-backed | N — purchase/cancel/copy/calendar controls are wired (`booking-page.tsx:59-104`) | N |
| `/community` | Filter groups; open group/event | `GET /community/groups`; client `public.api.ts:123-124`; handler `community.controller.ts:40-44` | Y — fallback image only (`community/page.tsx:18`); groups/counts are API-backed | N — category and links wired (`community/page.tsx:18`) | N |
| `/community/groups/[id]` | View posts/membership; join group; publish post; search discussion | `GET /community/groups/:id`; `GET .../membership`; `POST .../join`; `GET .../posts`; `POST /community/posts` (`public.api.ts:127-145`); handlers `community.controller.ts:47-108` | Y — date formatting/empty-state copy only (`groups/[id]/page.tsx:21-27,126-148`) | N — join and publish mutations wired (`groups/[id]/page.tsx:45-77,126,146-148`) | N |
| `/contact` | Choose branch/contact channel; send prefilled WhatsApp/email message; call/email branch | `GET /branches`; client `catalog.api.ts:13-15`; handler `branch.controller.ts:31-35`. Contact submission intentionally launches `wa.me`/`mailto:` from `SITE` (`contact/page.tsx:25-51`) | Y — site contact configuration and editorial copy (`contact/page.tsx:12,25-68`), not fake response data | N — form submit opens a real external channel and is disabled when none configured (`contact/page.tsx:42-51,102,164-168`) | N |
| `/events` | Filter events; inspect detail; register for free event | `GET /events`; `POST /events/:id/register`; client `public.api.ts:93-105`; handlers `event.controller.ts:44-50,85-91` | Y — category labels/fallback image (`events/page.tsx:20-31`); event/capacity records are API-backed | N — filter/detail/register actions wired; paid event button honestly disabled (`events/page.tsx:46,53-63`) | N |
| `/events/[id]` | Inspect event; register for free event | `GET /events/:id`; `POST /events/:id/register`; client `public.api.ts:100-105`; handlers `event.controller.ts:67-70,85-91` | Y — fallback image only (`events/[id]/page.tsx:23`) | N — register/login actions wired; paid registration visibly unavailable (`events/[id]/page.tsx:18,23`) | N |
| `/menu` | Select fulfillment/branch/category/tag; search; customize/add items; open cart | `GET /branches`, `GET /catalog?branchId` via `catalog.hooks.ts:13-52`; handlers `branch.controller.ts:31-35`, `catalog.controller.ts:42-54` | Y — quick-tag taxonomy/fallback imagery (`menu/page.tsx:39-49`, `catalog.api.ts:11,64`); products/prices are API-backed | N — all detected controls wired; sold-out button is intentionally disabled (`menu/page.tsx:230-252,280-288,322-397,599-619,655-665`) | N |
| `/account` | Edit profile/opt-in; derive favorites; reorder; view order history/branches; logout | `GET/PATCH /users/:id`; `GET /orders`; `GET /branches`; `GET /catalog`; `POST /auth/logout` (`account-page.tsx:41-51,141-157`); handlers `users.controller.ts:29-38,60+`, `orders.controller.ts:154-180`, `auth.controller.ts:175-196` | Y — tab definitions and display labels (`account-page.tsx:26-32`); account/order/catalog data are API-backed | N — profile, reorder, branch, and logout controls wired (`account-page.tsx:53-128,246-255,296-303,395-425`) | N |
| `/auth` | Legacy redirect to login | No API; Next redirect (`auth/page.tsx:1-4`) | N | N — redirect is wired | N |
| `/auth/callback` | Complete OAuth session restoration | `POST /auth/refresh`, `GET /auth/me`; client `auth/callback/page.tsx:7,15-26`; handlers `auth.controller.ts:88-110,199-203` | N | N — success/error navigation wired (`auth/callback/page.tsx:15-57`) | N |
| `/cart` | Change quantity/remove/clear; select fulfillment/split; request server quote; continue checkout | `GET /branches`; authenticated `POST /orders/quote`; guest `POST /orders/quote/guest` (`cart/page.tsx:72-101`, `orders.api.ts:40-57`); handlers `orders.controller.ts:93-123`, `guest-order-quotes.controller.ts:12+` | Y — fulfillment option labels only; totals are server-authoritative (`cart/page.tsx:89-101,303-322`) | N — cart/fulfillment/retry/checkout actions wired (`cart/page.tsx:141-432`) | N |
| `/checkout` | Confirm cart/fulfillment/voucher/points; quote; create idempotent order; start Midtrans | `GET /branches`, `GET /catalog`, `POST /orders/quote`, `POST /orders`, `POST /payments/midtrans/snap` (`checkout-page.tsx:28-38,12` and `orders.api.ts:40-78`); handlers `orders.controller.ts:56-123`, `payment.controller.ts:41-56` | Y — fulfillment/payment option labels (`checkout-page.tsx:17-19`); prices and order are API-backed | N — quote/order/payment mutations wired with visible errors | N |
| `/checkout/status` | Redirect provider return to authoritative order tracking | No direct API; Next redirect preserves order id (`checkout/status/page.tsx:7-11`), destination uses `GET /orders/:id` | N | N — redirect wired | N |
| `/checkout/success` | Redirect provider success return to authoritative order tracking | No direct API; Next redirect preserves order id (`checkout/success/page.tsx:7-11`), destination uses `GET /orders/:id` | N | N — redirect wired | N |
| `/login` | Password login; initiate Google OAuth; navigate to OTP/register | `POST /auth/login`; `GET /auth/google`; client `login/page.tsx:24-40,141-170`; handlers `auth.controller.ts:52-84,148-153` | Y — input placeholder/editorial copy only | N — password form and OAuth/OTP/register links wired (`login/page.tsx:24-40,71-189`) | N |
| `/loyalty` | View balance/tier/rewards/ledger; redeem reward; filter/export ledger | `GET /loyalty/me`; `GET /loyalty/rewards`; `GET /loyalty/transactions`; `POST /loyalty/rewards/:id/redeem` (`loyalty.api.ts:9-19`); handlers `loyalty.controller.ts:36-95` | Y — tier names/threshold presentation (`loyalty-page.tsx:20-21`); balance, server tier, rewards, ledger are API-backed | N — redeem/filter/export/pagination controls wired (`loyalty-page.tsx:47-55,238-278`) | N |
| `/offline` | Retry network load | No API; browser reload (`offline/page.tsx:7-10`) | Y — intentional offline explanatory state (`offline/page.tsx:13-33`) | N — retry wired (`offline/page.tsx:25-31`) | N |
| `/ops/kds` | Legacy redirect to admin kitchen | No API; Next redirect via configured admin URL (`ops/kds/page.tsx:1-5`) | N | N — redirect wired | N |
| `/ops/pos` | Legacy redirect to admin POS | No API; Next redirect (`ops/pos/page.tsx:1-5`) | N | N — redirect wired | N |
| `/ops/shift` | Legacy redirect to admin shifts | No API; Next redirect (`ops/shift/page.tsx:1-5`) | N | N — redirect wired | N |
| `/order/track/[orderId]` | Poll/live-track order/payment; retry payment; call waiter; download receipt; open feedback | `GET /orders/:id`; WebSocket order events; `POST /payments/midtrans/snap`; `POST /tables/:id/call`; `GET /branches` (`order-tracking-page.tsx:29-51`); handlers `orders.controller.ts:144-151`, `payment.controller.ts:41-56`, `table.controller.ts:146+` | Y — pipeline labels are presentation config (`order-tracking-page.tsx:20-26`); order state is API/WebSocket-backed | N — refresh/payment/waiter/receipt/feedback actions wired (`order-tracking-page.tsx:37-64`) | N |
| `/orders` | Search by order id; list own orders; open tracking | `GET /orders`; hook `orders.hooks.ts:15-22`; handler `orders.controller.ts:154-180` | Y — search example and empty-state copy only (`orders/page.tsx:57-77`) | N — search/retry/tracking links wired (`orders/page.tsx:23-26,57-188`) | N |
| `/orders/[id]` | Legacy redirect to tracking | No direct API; Next redirect (`orders/[id]/page.tsx:7-9`) | N | N — redirect wired | N |
| `/orders/[id]/thankyou` | Submit ratings/comment for completed owned order | `POST /orders/:id/feedback`; client `thankyou/page.tsx:45-65`; handler with ownership/completion validation `orders.controller.ts:196-210` | Y — feedback labels/editorial thank-you state only | N — rating and submit/home controls wired; submit disabled until all ratings selected (`thankyou/page.tsx:45-65,102-139`) | N |
| `/otp` | Send and verify OTP; restore profile | `POST /auth/otp/send`; `POST /auth/otp/verify`; `GET /auth/me` (`otp/page.tsx:23-59`); handlers `auth.controller.ts:114-144,199-203` | Y — placeholder/editorial copy only | N — send/verify/back actions wired (`otp/page.tsx:23-59,104-167`) | N |
| `/payment/status` | Redirect provider return to order tracking | No direct API; Next redirect (`payment/status/page.tsx:7-11`) | N | N — redirect wired | N |
| `/profile` | Legacy redirect to account | No API (`profile/page.tsx:1-4`) | N | N — redirect wired | N |
| `/qr/[code]` | Resolve table QR then enter table menu | `GET /tables/qr/:code`; client `public.api.ts:152-153`; handler `table.controller.ts:33-40` | N | N — query retry/redirect wired (`qr/[code]/page.tsx:17-41`) | N |
| `/register` | Register password account and marketing opt-in; navigate to login/OTP | `POST /auth/register`; client `register/page.tsx:40-57`; handler `auth.controller.ts:41-48` | Y — placeholder/editorial copy only | N — form and navigation wired (`register/page.tsx:34-57,111-283`) | N |
| `/reservations` | Legacy redirect to booking | No API (`reservations/page.tsx:1-4`) | N | N — redirect wired | N |
| `/table/[tableId]` | Load table/catalog; search/customize/add; call waiter/request bill; checkout | `GET /tables/public/:id`; `GET /branches`; `GET /catalog?branchId`; `POST /tables/:id/call` (`table/[tableId]/page.tsx:39-54`); handlers `table.controller.ts:43-60,146+`, `catalog.controller.ts:42-54` | Y — waiter-call option labels/status map (`table/[tableId]/page.tsx:18-30`); tables/products are API-backed | N — search/cart/checkout/customizer/waiter actions wired (`table/[tableId]/page.tsx:54-120`) | N |

## Admin routes (21)

| Route | Primary user action(s) on page | Data source (API endpoint(s) hit, verified by grep + reading the handler) | Static/mock content present? (Y/N + line ref) | Buttons/forms with no-op or unwired handlers (Y/N + line ref) | Manually exercised end-to-end in dev environment? (Y/N) |
|---|---|---|---|---|---|
| `/` | View operational overview; open orders/inventory; refresh | `GET /auth/me`, `/branches`, `/orders`, `/catalog/branch_products`, `/analytics/revenue`, `/events/manage` (`(dashboard)/page.tsx:20-30`); matching identity/branch/order/catalog/analytics/event handlers | Y — KPI labels only; values are API-backed (`(dashboard)/page.tsx:35-170`) | N — reload/navigation wired (`page.tsx:62,133,170`) | N |
| `/analytics` | Select branch; refresh; export CSV/JSON | `GET /analytics/revenue`, `/analytics/categories`, `/analytics/customers`, plus branch scope (`analytics/page.tsx:43-45,112`); handlers `analytics.controller.ts:16-40` | Y — chart palette/timeframe labels only; metrics are API-backed | N — branch/refresh/export actions wired (`analytics/page.tsx:238-254`) | N |
| `/branches` | Select/search branch; edit capacity; edit branch-product price/availability | `GET/PATCH /branches`; `GET /catalog/branch_products`; `PATCH /branches/:branchId/products/:productId` and `/availability` (`branches/page.tsx:50-96,177,231`); handlers `branch.controller.ts:31-49+`, `catalog.controller.ts:57-74,121-157+` | N — table/matrix is API-backed | N — save/toggle/retry controls wired (`branches/page.tsx:264-491`) | N |
| `/community` | Create group; list groups/recent posts; delete post | `GET/POST /community/groups`; `GET /community/posts/manage`; `DELETE /community/posts/:id` (`community/page.tsx:25-62`); handlers `community.controller.ts:32-44,86-115+` | N | N — create/cancel/delete actions wired (`community/page.tsx:46-89`) | N |
| `/crm` | Search/filter customers; generate persisted retention campaign draft | `GET /analytics/customers`; `POST /marketing/campaigns`; branch scope (`crm/page.tsx:44-78,131`); handlers `analytics.controller.ts:36-40+`, `marketing.controller.ts:50-61` | Y — cohort definitions/default campaign copy are form configuration, not customer records | N — refresh/cohort/create-draft controls wired (`crm/page.tsx:186-341`) | N |
| `/events` | Create free event; filter/list; change status; open detail | `GET /events/manage`; `POST /events`; `PATCH /events/:id`; branch scope (`events/page.tsx:32-91`); handlers `event.controller.ts:33-82` | Y — empty form/category choices only; records are API-backed | N — create/cancel/status/detail controls wired (`events/page.tsx:119-373`) | N |
| `/events/[id]` | Edit event; inspect/update registrations | `GET/PATCH /events/:id`; `GET/PATCH /events/:id/registrations/:registrationId` (`events/[id]/page.tsx:27-100`); handlers `event.controller.ts:67-82,94-113+` | N | N — save and registration transition controls wired (`events/[id]/page.tsx:72-326`) | N |
| `/inventory` | Select/search branch inventory; edit stock/capacity/threshold/supplier/burn rate | `GET /catalog/branch_products`; `PATCH /branches/:branchId/products/:productId`; branch scope (`inventory/page.tsx:87-110,178`); handlers `catalog.controller.ts:57-74,149+` | N — stock records are API-backed | N — editor/save/cancel/retry controls wired (`inventory/page.tsx:241-498`) | N |
| `/loyalty` | Create/edit/toggle rewards | `GET /loyalty/rewards/manage`; `POST /loyalty/rewards`; `PATCH /loyalty/rewards/:id` (`loyalty/page.tsx:26-69`); handlers `loyalty.controller.ts:62-83` | Y — empty reward form/tier choices only; rewards are API-backed | N — create/edit/toggle controls wired (`loyalty/page.tsx:81-97`) | N |
| `/marketing` | Create/update/test/dispatch campaign; inspect provider status | `GET/POST/PATCH /marketing/campaigns`; `POST .../test`; `POST .../dispatch`; `GET /marketing/provider-status`; branch scope (`marketing/page.tsx:63-103,156-211`); handlers `marketing.controller.ts:31-96+` | Y — initial form defaults/message template (`marketing/page.tsx:39-48`), not fake provider/delivery records | N — reset/reload/save/test/broadcast/edit controls wired (`marketing/page.tsx:299-631`) | N |
| `/orders` | Search/filter orders; inspect details; advance status | `GET /orders`; `PATCH /orders/:id/status`; branch scope (`orders/page.tsx:19-54`); handlers `orders.controller.ts:154-193` | Y — status transition map/filter choices only; orders are API-backed | N — reload/detail/status controls wired (`orders/page.tsx:66-80`) | N |
| `/products` | Search/filter/create/edit products | `GET/POST/PATCH /products`; `GET /categories`; `GET /auth/me` (`products/page.tsx:28-93`); handlers `catalog.controller.ts:77-118`, `auth.controller.ts:199-203` | Y — empty product form only; products/categories are API-backed | N — create/edit/save/cancel controls wired (`products/page.tsx:57-133`) | N |
| `/reservations` | Filter/list reservations; transition status | `GET /reservations`; `PATCH /reservations/:id/status`; branch scope (`reservations/page.tsx:20-43`); handlers `reservations.controller.ts:68-108` | Y — allowed transition map only; reservations are API-backed | N — reload/status controls wired (`reservations/page.tsx:59-74`) | N |
| `/settings` | Navigate to branch/provider/analytics configuration surfaces; view build API configuration | No operational mutation. Build-time `NEXT_PUBLIC_API_URL` check (`settings/page.tsx:3-9`) | Y — explanatory/build configuration content (`settings/page.tsx:3-9`), not mocked connection status | N — navigation links wired (`settings/page.tsx:8-9`) | N |
| `/shifts` | View current/history; open shift; record cash movement; close/balance shift | `GET /shifts/current`; `GET /shifts`; `POST /shifts/open`; `POST /shifts/:id/movements`; `POST /shifts/:id/close` (`shifts/page.tsx:30-59`); handlers `shift.controller.ts:35-99+` | Y — mode labels/allowed cash movement types only; balances are server-calculated | N — open/movement/close/reload form wired (`shifts/page.tsx:73-84`) | N |
| `/users` | Search/filter users; award points with reason | `GET /users`; `POST /loyalty/users/:id/award` (`users/page.tsx:21-46`); handlers `users.controller.ts:41-57`, `loyalty.controller.ts:98+` | Y — role filter choices only; users/points are API-backed | N — search/award/cancel actions wired (`users/page.tsx:64-78`) | N |
| `/kitchen` | View branch tickets; live-update; advance KDS status | `GET /orders`; `PATCH /orders/:id/status`; WebSocket (`kitchen/page.tsx:38-62`); handlers `orders.controller.ts:154-193` | Y — KDS status columns/SLA display configuration only; tickets are API-backed | N — status actions wired (`kitchen/page.tsx:62+`) | N |
| `/login` | Authenticate staff/admin | `POST /auth/login` through `adminLogin`; handler `auth.controller.ts:52-84` | Y — placeholders/editorial copy only | N — form/password toggle wired (`login/page.tsx:67-110`) | N |
| `/pos` | Select branch/table/products; obtain server quote; create idempotent order; settle cash | `GET /catalog/branch_products`; `GET /tables/branch/:id`; `GET /shifts/current`; `POST /orders/quote`; `POST /orders`; `POST /payments/cash` (`pos/page.tsx:39-41,90,174+`); handlers catalog/table/shift/order/cash-payment controllers | Y — fulfillment labels only; products/quote/order/payment are API-backed | N — product/cart/quote/checkout controls wired (`pos/page.tsx:90-200+`) | N |
| `/pos/shifts` | Legacy redirect to `/shifts` | No API (`pos/shifts/page.tsx:1-4`) | N | N — redirect wired | N |
| `/tables` | View/update table status; receive/resolve waiter calls | `GET /tables/branch/:id`; `GET .../calls`; `PATCH /tables/:id/status`; `PATCH /tables/calls/:id/resolve`; WebSocket (`tables/page.tsx:41-80`); handlers `table.controller.ts:63-143` | Y — transition map/status labels only; tables/calls are API-backed | N — status/resolve/retry controls wired (`tables/page.tsx:66-99`) | N |

## Confirmed cross-route findings and resolutions

1. **[VERIFIED-WITH-EVIDENCE] Barista reachability.** The marketing layout mounts `BaristaConciergeModal`; the layout regression test passes.
2. **[VERIFIED-WITH-EVIDENCE] Branch scope.** Both recommendation POST bodies carry the active `branchId`; the component test asserts both requests.
3. **[VERIFIED-WITH-EVIDENCE] Product truth.** Owner option `(a)` is recorded in [AI_FEATURE_DECISION.md](./AI_FEATURE_DECISION.md), and customer copy now uses **Rekomendasi Barista**.
4. **[VERIFIED-WITH-EVIDENCE / LOCAL] Runtime prerequisites.** The isolated `warkop_audit` database was migrated, persistence tests passed `7/7 + 3/3`, Playwright passed `10/10`, and UI audit reported `66/66` with zero failures.
5. **[VERIFIED-WITH-EVIDENCE / LOCAL] Payment exception UX.** The tracking page uses the existing protected payment-status endpoint and unit tests cover pending, paid, expired, gateway failure, persisted failure, refunded, and cancelled states.
6. **[VERIFIED-WITH-EVIDENCE / LOCAL] Shift and sale flow.** Playwright proves open → movement → cash POS sale → KDS → completed order → close/balance → analytics, including persisted exact totals.

## Phase 0 gate status

- Inventory/source trace: **verified-with-evidence** for all 56 page files.
- Local install/typecheck/lint/build/scope gate: **verified-with-evidence**.
- Exact baseline CI: **verified-with-evidence** — [GitHub Actions CI #49](https://github.com/MasRizqi07/warkop-yareh/actions/runs/34886618344), status `Success`, head SHA `a12d52283a087e4f2fad56748b4452b8d83d10a5`.
- Unit/integration aggregate after fixture setup: **verified-with-evidence locally** — web `43/43`, admin `4/4`, API `259/259`, checkout persistence `7/7`, operations persistence `3/3`.
- Browser/UI runtime: **verified-with-evidence locally** — Playwright `10/10`; UI audit `66/66` with no failures.
- Phase 1-4 implementation scope: **verified-with-evidence locally**; see [PHASE1_4_EVIDENCE.md](./PHASE1_4_EVIDENCE.md).
- Exact candidate CI and real staging primary-flow walkthroughs: **not established by local evidence**.
- Phase 5 release acceptance: **blocked pending exact-commit CI, staging proof, and Rizqi's required owner sign-off**.
